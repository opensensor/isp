#include "include/main.h"


  int32_t dump_csi_reg(void* arg1)

{
    int32_t entry_$a2;
    isp_printf(); // Fixed: macro call, removed arguments;
    isp_printf(); // Fixed: macro call, removed arguments);
    isp_printf(); // Fixed: macro call, removed arguments + 4));
    isp_printf(); // Fixed: macro call, removed arguments + 8));
    isp_printf(); // Fixed: macro call, removed arguments + 0xc));
    isp_printf(); // Fixed: macro call, removed arguments + 0x10));
    isp_printf(); // Fixed: macro call, removed arguments\n", *(*(arg1 + 0xb8) + 0x14));
    isp_printf(); // Fixed: macro call, removed arguments + 0x18));
    isp_printf(); // Fixed: macro call, removed arguments + 0x1c));
    isp_printf(); // Fixed: macro call, removed arguments + 0x20));
    isp_printf(); // Fixed: macro call, removed arguments + 0x24));
    isp_printf(); // Fixed: macro call, removed arguments + 0x28));
    isp_printf(); // Fixed: macro call, removed arguments + 0x2c));
    isp_printf(); // Fixed: macro call, removed arguments + 0x30));
    /* tailcall */
    return isp_printf(); // Fixed: macro call, removed arguments + 0x34));
}

